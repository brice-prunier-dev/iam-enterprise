# joe-fx

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build joe-fx` to build the library.

## Running unit tests

Run `nx test joe-fx` to execute the unit tests via [Jest](https://jestjs.io).
